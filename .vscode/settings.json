{   
    // im too tired of seeing the red dot
    // if ur using vscode, remove below settings to see the deprecated folder
    "files.exclude": {
        "deprecated/": true
    },
    "search.exclude": {
        "deprecated/": true
    }
}